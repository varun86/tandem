export function openDriftDrawerIfNeeded(hasDrift: boolean): boolean {
  return hasDrift;
}

export function closeDrawersOnEsc(isDriftDrawerOpen: boolean): boolean {
  return isDriftDrawerOpen ? false : isDriftDrawerOpen;
}
